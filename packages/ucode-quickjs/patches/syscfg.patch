diff --git a/quickjs/Makefile b/quickjs/Makefile
index dd6b51d..595d612 100644
--- a/quickjs/Makefile
+++ b/quickjs/Makefile
@@ -4,16 +4,16 @@
 # Include platform-specific configuration
 include Makefile.platform
 
-# Installation prefix - defaults to Homebrew on macOS, /usr/local on Linux
+# Installation prefix - defaults to Homebrew on macOS, /usr on Linux
 ifeq ($(UNAME_S),Darwin)
     PREFIX ?= /opt/homebrew
 else
-    PREFIX ?= /usr/local
+    PREFIX ?= /usr
 endif
 
 # Common compiler flags
-CFLAGS = -Wall -O2 -I/usr/local/include -I/usr/include -I./include $(CFLAGS_PLATFORM)
-LDFLAGS = -L/usr/local/lib -L/usr/local/lib/quickjs -lquickjs
+CFLAGS = -Wall -O2 -I/usr/include -I/usr/include -I./include $(CFLAGS_PLATFORM)
+LDFLAGS = -L/usr/lib -L/usr/lib -lquickjs
 
 # Add platform-specific library paths (for Homebrew on macOS)
 ifeq ($(UNAME_S),Darwin)
@@ -62,12 +62,12 @@ ifneq ($(HAS_ULOOP),)
     $(info Building with filewatch support)
 endif
 
-# Optional sysconfig module (add if available)
+# Optional sysconfig module (add if available) - uses utopia syscfg
 ifneq ($(HAS_SYSCONFIG),)
     MODULE_SRCS += ucode-sysconfig.c
-    LDFLAGS += -lsysconfig
-    LDFLAGS += $(RPATH_FLAG)/usr/local/lib
-    $(info Building with sysconfig support)
+    LDFLAGS += -lsyscfg
+    LDFLAGS += $(RPATH_FLAG)/usr/lib
+    $(info Building with sysconfig support (utopia syscfg))
 endif
 
 # Object files
@@ -83,7 +83,7 @@ all: $(TARGET_LIB) $(TARGET_CLI)
 
 # Build shared library (includes modules, core, and library wrapper)
 $(TARGET_LIB): $(MODULE_OBJS) $(CORE_OBJS) $(LIB_OBJS)
-	$(CC) $(LDFLAGS_PLATFORM) -install_name $(PREFIX)/lib/$(TARGET_LIB_FULL) -o $(TARGET_LIB_FULL) $(MODULE_OBJS) $(CORE_OBJS) $(LIB_OBJS) $(LDFLAGS)
+	$(CC) $(LDFLAGS_PLATFORM) -o $(TARGET_LIB_FULL) $(MODULE_OBJS) $(CORE_OBJS) $(LIB_OBJS) $(LDFLAGS)
 	ln -sf $(TARGET_LIB_FULL) $(TARGET_LIB_SONAME)
 	ln -sf $(TARGET_LIB_SONAME) $(TARGET_LIB)
 
@@ -96,56 +96,56 @@ clean:
 
 install: $(TARGET_LIB) $(TARGET_CLI)
 	# Install library
-	install -d $(PREFIX)/lib
-	install -m 755 $(TARGET_LIB_FULL) $(PREFIX)/lib/
-	ln -sf $(TARGET_LIB_FULL) $(PREFIX)/lib/$(TARGET_LIB_SONAME)
-	ln -sf $(TARGET_LIB_SONAME) $(PREFIX)/lib/$(TARGET_LIB)
+	install -d "$(DESTDIR)$(PREFIX)/lib"
+	install -m 755 $(TARGET_LIB_FULL) "$(DESTDIR)$(PREFIX)/lib/"
+	ln -sf $(TARGET_LIB_FULL) $(DESTDIR)$(PREFIX)/lib/$(TARGET_LIB_SONAME)
+	ln -sf $(TARGET_LIB_SONAME) $(DESTDIR)$(PREFIX)/lib/$(TARGET_LIB)
 	# Install CLI (as ucode-qjs only, do not overwrite existing ucode binary)
-	install -d $(PREFIX)/bin
-	install -m 755 $(TARGET_CLI) $(PREFIX)/bin/
+	install -d "$(DESTDIR)$(PREFIX)/bin"
+	install -m 755 $(TARGET_CLI) "$(DESTDIR)$(PREFIX)/bin/"ucode-qjs
 	# Install headers (if they exist)
 	@if [ -d ../include/ucode ]; then \
-	    install -d $(PREFIX)/include/ucode; \
-	    install -m 644 ../include/ucode/ucode.h $(PREFIX)/include/ucode/ || true; \
-	    install -m 644 ../include/ucode/module.h $(PREFIX)/include/ucode/ || true; \
+	    install -d "$(DESTDIR)$(PREFIX)/include/ucode"; \
+	    install -m 644 ../include/ucode/ucode.h "$(DESTDIR)$(PREFIX)/include/ucode/" || true; \
+	    install -m 644 ../include/ucode/module.h "$(DESTDIR)$(PREFIX)/include/ucode/" || true; \
 	fi
 	# Generate and install pkg-config file (if template exists)
 	@if [ -f ../debian/ucode-quickjs.pc.in ]; then \
-	    install -d $(PREFIX)/lib/pkgconfig; \
+	    install -d "$(DESTDIR)$(PREFIX)/lib/pkgconfig"; \
 	    sed -e 's|@PREFIX@|$(PREFIX)|g' \
 	        -e 's|@EXEC_PREFIX@|$(PREFIX)|g' \
 	        -e 's|@LIBDIR@|$(PREFIX)/lib|g' \
 	        -e 's|@INCLUDEDIR@|$(PREFIX)/include|g' \
 	        -e 's|@VERSION@|1.0.0|g' \
-	        ../debian/ucode-quickjs.pc.in > $(PREFIX)/lib/pkgconfig/ucode-quickjs.pc; \
+	        ../debian/ucode-quickjs.pc.in > $(DESTDIR)$(PREFIX)/lib/pkgconfig/ucode-quickjs.pc; \
 	fi
 	# Create module directory
-	install -d $(PREFIX)/lib/ucode
+	install -d "$(DESTDIR)$(PREFIX)/lib/ucode"
 	# Install documentation
-	install -d $(PREFIX)/share/doc/ucode-qjs
+	install -d "$(DESTDIR)$(PREFIX)/share/doc/ucode-qjs"
 	@if [ -d ../docs ]; then \
-	    install -m 644 ../docs/*.md $(PREFIX)/share/doc/ucode-qjs/ 2>/dev/null || true; \
+	    install -m 644 ../docs/*.md "$(DESTDIR)$(PREFIX)/share/doc/ucode-qjs/" 2>/dev/null || true; \
 	fi
 	@if [ -f ../README.md ]; then \
-	    install -m 644 ../README.md $(PREFIX)/share/doc/ucode-qjs/; \
+	    install -m 644 ../README.md "$(DESTDIR)$(PREFIX)/share/doc/ucode-qjs/"; \
 	fi
 	@if [ -f ../CHANGELOG.md ]; then \
-	    install -m 644 ../CHANGELOG.md $(PREFIX)/share/doc/ucode-qjs/; \
+	    install -m 644 ../CHANGELOG.md "$(DESTDIR)$(PREFIX)/share/doc/ucode-qjs/"; \
 	fi
 	# Install examples
-	install -d $(PREFIX)/share/ucode-qjs/examples
+	install -d "$(DESTDIR)$(PREFIX)/share/ucode-qjs/examples"
 	@if [ -d ../examples ]; then \
-	    cp -r ../examples/* $(PREFIX)/share/ucode-qjs/examples/ 2>/dev/null || true; \
+	    cp -r ../examples/* "$(DESTDIR)$(PREFIX)/share/ucode-qjs/examples/" 2>/dev/null || true; \
 	fi
 	# Install compatibility layer and templates
 	@if [ -f ucode-compat.js ]; then \
-	    install -m 644 ucode-compat.js $(PREFIX)/lib/ucode/; \
+	    install -m 644 ucode-compat.js "$(DESTDIR)$(PREFIX)/lib/ucode/"; \
 	fi
 	@if [ -f ucode-template.js ]; then \
-	    install -m 644 ucode-template.js $(PREFIX)/lib/ucode/; \
+	    install -m 644 ucode-template.js "$(DESTDIR)$(PREFIX)/lib/ucode/"; \
 	fi
 	@if [ -f ucode-tr181.uc ]; then \
-	    install -m 644 ucode-tr181.uc $(PREFIX)/lib/ucode/; \
+	    install -m 644 ucode-tr181.uc "$(DESTDIR)$(PREFIX)/lib/ucode/"; \
 	fi
 	# Run ldconfig (only on Linux)
 ifeq ($(UNAME_S),Linux)
diff --git a/quickjs/Makefile.platform b/quickjs/Makefile.platform
index 6fa41e8..ca6fcda 100644
--- a/quickjs/Makefile.platform
+++ b/quickjs/Makefile.platform
@@ -40,11 +40,11 @@ ifeq ($(UNAME_S),Darwin)
         MODULES_AVAILABLE += rbus
     endif
     
-    # Check for sysconfig (skip if SKIP_SYSCONFIG is set)
+    # Check for sysconfig (skip if SKIP_SYSCONFIG is set) - uses utopia syscfg
     ifndef SKIP_SYSCONFIG
         # Only enable if both library and header are accessible
-        ifneq ($(wildcard /usr/local/lib/libsysconfig.*),)
-            ifneq ($(wildcard /usr/local/include/sysconfig/sysconfig.h),)
+        ifneq ($(wildcard /usr/lib/libsyscfg.*),)
+            ifneq ($(wildcard /usr/include/syscfg/syscfg.h),)
                 MODULES_AVAILABLE += sysconfig
                 CFLAGS_PLATFORM += -I/usr/local/include
                 LDFLAGS_PLATFORM += -L/usr/local/lib
diff --git a/quickjs/ucode-sysconfig.c b/quickjs/ucode-sysconfig.c
index 99acba1..1512c69 100644
--- a/quickjs/ucode-sysconfig.c
+++ b/quickjs/ucode-sysconfig.c
@@ -1,26 +1,28 @@
 /**
  * ucode-sysconfig.c - Sysconfig module for ucode/QuickJS
  * 
- * Provides bindings for the sysconfig library with both contextual
+ * Provides bindings for the utopia syscfg library with both contextual
  * and non-contextual APIs for UCI-compatible configuration management.
  * 
- * Non-contextual API: Pass database explicitly each time
- *   sysconfig.get(db, section, key)
- *   sysconfig.set(db, section, key, value)
+ * Non-contextual API: Pass namespace explicitly each time
+ *   sysconfig.get(ns, key)
+ *   sysconfig.set(ns, key, value)
  * 
- * Contextual API: Bind to a database, then omit it
- *   ctx = sysconfig.context(db)
- *   ctx.get(section, key)
- *   ctx.set(section, key, value)
+ * Contextual API: Bind to a namespace, then omit it
+ *   ctx = sysconfig.context(ns)
+ *   ctx.get(key)
+ *   ctx.set(key, value)
  */
 
 #include <stdio.h>
 #include <stdlib.h>
 #include <string.h>
-#include <sysconfig/sysconfig.h>
+#include <errno.h>
+#include <syscfg/syscfg.h>
 #include <quickjs/quickjs.h>
 
 #define MODULE_NAME "sysconfig"
+#define SYSCFG_MAX_VALUE_LEN 256
 
 // Error message buffer
 static char g_error_msg[256] = {0};
@@ -30,7 +32,7 @@ static JSClassID context_class_id = 0;
 
 // Context handle structure
 typedef struct {
-    char* database;
+    char* namespace_prefix;
 } sysconfig_context_t;
 
 // Helper: Set error message
@@ -38,10 +40,23 @@ static void set_error(const char* msg) {
     snprintf(g_error_msg, sizeof(g_error_msg), "%s", msg);
 }
 
-// Helper: Convert sysconfig error to string
+// Helper: Convert syscfg error to string
 static const char* error_to_string(int err) {
-    const char* msg = sysconfig_strerror(err);
-    return msg ? msg : "Unknown error";
+    switch (err) {
+        case 0: return "Success";
+        case ERR_INVALID_PARAM: return "Invalid parameter";
+        case ERR_NOT_INITIALIZED: return "Not initialized";
+        case ERR_MEM_ALLOC: return "Memory allocation failed";
+        case ERR_INVALID_STATE: return "Invalid state";
+        case ERR_NO_SPACE: return "No space";
+        case ERR_ENTRY_TOO_BIG: return "Entry too big";
+        case ERR_IO_FAILURE: return "IO failure";
+        case ERR_IO_FILE_OPEN: return "Cannot open file";
+        case ERR_IO_FILE_STAT: return "Cannot stat file";
+        case ERR_IO_FILE_TOO_BIG: return "File too big";
+        case ERR_IO_FILE_WRITE: return "Cannot write file";
+        default: return "Unknown error";
+    }
 }
 
 // Helper: Build namespace from db + section
@@ -71,31 +86,12 @@ static char* make_namespace(const char* db, const char* section) {
 
 /**
  * sysconfig.init([config_file])
- * Initialize the sysconfig system
+ * Initialize the sysconfig system (no-op for utopia syscfg)
  */
 static JSValue js_sysconfig_init(JSContext *ctx, JSValueConst this_val,
                                  int argc, JSValueConst *argv)
 {
-    const char* config_file = NULL;
-    
-    if (argc > 0 && !JS_IsNull(argv[0]) && !JS_IsUndefined(argv[0])) {
-        config_file = JS_ToCString(ctx, argv[0]);
-        if (!config_file) {
-            return JS_EXCEPTION;
-        }
-    }
-    
-    int ret = sysconfig_init(config_file);
-    
-    if (config_file) {
-        JS_FreeCString(ctx, config_file);
-    }
-    
-    if (ret != SYSCONFIG_SUCCESS) {
-        set_error(error_to_string(ret));
-        return JS_FALSE;
-    }
-    
+    // utopia syscfg is automatically initialized, no explicit init needed
     return JS_TRUE;
 }
 
@@ -106,7 +102,9 @@ static JSValue js_sysconfig_init(JSContext *ctx, JSValueConst this_val,
 static JSValue js_sysconfig_cleanup(JSContext *ctx, JSValueConst this_val,
                                     int argc, JSValueConst *argv)
 {
-    sysconfig_cleanup();
+    // Note: syscfg_destroy() should only be called during system shutdown
+    // Not recommended for normal operation
+    syscfg_destroy();
     return JS_UNDEFINED;
 }
 
@@ -117,8 +115,8 @@ static JSValue js_sysconfig_cleanup(JSContext *ctx, JSValueConst this_val,
 static JSValue js_sysconfig_is_initialized(JSContext *ctx, JSValueConst this_val,
                                            int argc, JSValueConst *argv)
 {
-    int initialized = sysconfig_is_initialized();
-    return JS_NewBool(ctx, initialized);
+    // utopia syscfg is always initialized automatically
+    return JS_TRUE;
 }
 
 /**
@@ -157,21 +155,19 @@ static JSValue js_sysconfig_get(JSContext *ctx, JSValueConst this_val,
     }
     
     char* namespace = make_namespace(db, section);
-    char value[SYSCONFIG_MAX_VALUE_LEN];
+    char value[SYSCFG_MAX_VALUE_LEN];
     memset(value, 0, sizeof(value));
     
-    int ret = sysconfig_get(namespace, key, value, sizeof(value));
+    int ret = syscfg_get(namespace, key, value, sizeof(value));
     
     if (db) JS_FreeCString(ctx, db);
     if (section) JS_FreeCString(ctx, section);
     JS_FreeCString(ctx, key);
     if (namespace) free(namespace);
     
-    if (ret == SYSCONFIG_ERROR_NOT_FOUND) {
+    if (ret != 0) {
+        // Non-zero return means error or not found - return null for not found
         return JS_NULL;
-    } else if (ret != SYSCONFIG_SUCCESS) {
-        set_error(error_to_string(ret));
-        return JS_ThrowInternalError(ctx, "%s", g_error_msg);
     }
     
     return JS_NewString(ctx, value);
@@ -218,20 +214,25 @@ static JSValue js_sysconfig_get_default(JSContext *ctx, JSValueConst this_val,
     }
     
     char* namespace = make_namespace(db, section);
-    char value[SYSCONFIG_MAX_VALUE_LEN];
-    int ret = sysconfig_get_default(namespace, key, default_value, value, sizeof(value));
+    char value[SYSCFG_MAX_VALUE_LEN];
+    memset(value, 0, sizeof(value));
+    
+    // utopia syscfg doesn't have get_default, so we try get and use default on failure
+    int ret = syscfg_get(namespace, key, value, sizeof(value));
     
     if (db) JS_FreeCString(ctx, db);
     if (section) JS_FreeCString(ctx, section);
     JS_FreeCString(ctx, key);
-    JS_FreeCString(ctx, default_value);
     if (namespace) free(namespace);
     
-    if (ret != SYSCONFIG_SUCCESS) {
-        set_error(error_to_string(ret));
-        return JS_ThrowInternalError(ctx, "%s", g_error_msg);
+    if (ret != 0) {
+        // Failed to get value, return default
+        JSValue result = JS_NewString(ctx, default_value);
+        JS_FreeCString(ctx, default_value);
+        return result;
     }
     
+    JS_FreeCString(ctx, default_value);
     return JS_NewString(ctx, value);
 }
 
@@ -284,7 +285,7 @@ static JSValue js_sysconfig_set(JSContext *ctx, JSValueConst this_val,
     
     char* namespace = make_namespace(db, section);
     
-    int ret = sysconfig_set(namespace, key, value);
+    int ret = syscfg_set(namespace, key, value);
     
     if (db) JS_FreeCString(ctx, db);
     if (section) JS_FreeCString(ctx, section);
@@ -292,7 +293,7 @@ static JSValue js_sysconfig_set(JSContext *ctx, JSValueConst this_val,
     if (value) JS_FreeCString(ctx, value);
     if (namespace) free(namespace);
     
-    if (ret != SYSCONFIG_SUCCESS) {
+    if (ret != 0) {
         set_error(error_to_string(ret));
         return JS_ThrowInternalError(ctx, "%s", g_error_msg);
     }
@@ -336,14 +337,14 @@ static JSValue js_sysconfig_unset(JSContext *ctx, JSValueConst this_val,
     }
     
     char* namespace = make_namespace(db, section);
-    int ret = sysconfig_unset(namespace, key);
+    int ret = syscfg_unset(namespace, key);
     
     if (db) JS_FreeCString(ctx, db);
     if (section) JS_FreeCString(ctx, section);
     JS_FreeCString(ctx, key);
     if (namespace) free(namespace);
     
-    if (ret != SYSCONFIG_SUCCESS) {
+    if (ret != 0) {
         set_error(error_to_string(ret));
         return JS_FALSE;
     }
@@ -387,18 +388,17 @@ static JSValue js_sysconfig_exists(JSContext *ctx, JSValueConst this_val,
     }
     
     char* namespace = make_namespace(db, section);
-    int ret = sysconfig_exists(namespace, key);
+    
+    // utopia syscfg doesn't have exists(), so we try a get instead
+    char value[SYSCFG_MAX_VALUE_LEN];
+    int ret = syscfg_get(namespace, key, value, sizeof(value));
     
     if (db) JS_FreeCString(ctx, db);
     if (section) JS_FreeCString(ctx, section);
     JS_FreeCString(ctx, key);
     if (namespace) free(namespace);
     
-    if (ret < 0) {
-        return JS_FALSE;
-    }
-    
-    return JS_NewBool(ctx, ret);
+    return JS_NewBool(ctx, ret == 0);
 }
 
 /**
@@ -409,10 +409,10 @@ static JSValue js_sysconfig_exists(JSContext *ctx, JSValueConst this_val,
 static JSValue js_sysconfig_commit(JSContext *ctx, JSValueConst this_val,
                                    int argc, JSValueConst *argv)
 {
-    // Note: sysconfig library commits globally, db parameter ignored for now
-    int ret = sysconfig_commit();
+    // syscfg commits globally, db parameter ignored
+    int ret = syscfg_commit();
     
-    if (ret != SYSCONFIG_SUCCESS) {
+    if (ret != 0) {
         set_error(error_to_string(ret));
         return JS_FALSE;
     }
@@ -427,14 +427,9 @@ static JSValue js_sysconfig_commit(JSContext *ctx, JSValueConst this_val,
 static JSValue js_sysconfig_rollback(JSContext *ctx, JSValueConst this_val,
                                      int argc, JSValueConst *argv)
 {
-    int ret = sysconfig_rollback();
-    
-    if (ret != SYSCONFIG_SUCCESS) {
-        set_error(error_to_string(ret));
-        return JS_FALSE;
-    }
-    
-    return JS_TRUE;
+    // utopia syscfg doesn't have rollback - would need to reload from file
+    // For now, return an error
+    return JS_ThrowInternalError(ctx, "rollback() not supported by utopia syscfg");
 }
 
 /**
@@ -444,13 +439,8 @@ static JSValue js_sysconfig_rollback(JSContext *ctx, JSValueConst this_val,
 static JSValue js_sysconfig_is_dirty(JSContext *ctx, JSValueConst this_val,
                                      int argc, JSValueConst *argv)
 {
-    int ret = sysconfig_is_dirty();
-    
-    if (ret < 0) {
-        return JS_FALSE;
-    }
-    
-    return JS_NewBool(ctx, ret);
+    // utopia syscfg doesn't track dirty state - return false
+    return JS_FALSE;
 }
 
 // Iterator callback structure
@@ -539,25 +529,15 @@ static JSValue js_sysconfig_iterate(JSContext *ctx, JSValueConst this_val,
     
     char* namespace = make_namespace(db, section);
     
-    iterator_data_t data = {
-        .ctx = ctx,
-        .callback = argv[3],
-        .should_stop = 0
-    };
-    
-    int ret = sysconfig_iterate(namespace, pattern, iterator_callback, &data);
+    // utopia syscfg doesn't support iterate - would need to implement using syscfg_getall
+    // For now, return an error
     
     if (db) JS_FreeCString(ctx, db);
     if (section) JS_FreeCString(ctx, section);
     if (pattern) JS_FreeCString(ctx, pattern);
     if (namespace) free(namespace);
     
-    if (ret != SYSCONFIG_SUCCESS) {
-        set_error(error_to_string(ret));
-        return JS_FALSE;
-    }
-    
-    return JS_TRUE;
+    return JS_ThrowInternalError(ctx, "iterate() not supported by utopia syscfg");
 }
 
 /**
@@ -588,17 +568,14 @@ static JSValue js_sysconfig_count(JSContext *ctx, JSValueConst this_val,
     }
     
     char* namespace = make_namespace(db, section);
-    int ret = sysconfig_count(namespace);
+    
+    // utopia syscfg doesn't support count - return 0
+    int ret = 0;
     
     if (db) JS_FreeCString(ctx, db);
     if (section) JS_FreeCString(ctx, section);
     if (namespace) free(namespace);
     
-    if (ret < 0) {
-        set_error(error_to_string(ret));
-        return JS_NewInt32(ctx, 0);
-    }
-    
     return JS_NewInt32(ctx, ret);
 }
 
@@ -624,16 +601,10 @@ static JSValue js_sysconfig_import_json(JSContext *ctx, JSValueConst this_val,
         return JS_EXCEPTION;
     }
     
-    int ret = sysconfig_import_json(json_data, merge);
-    
+    // utopia syscfg doesn't support JSON import
     JS_FreeCString(ctx, json_data);
     
-    if (ret != SYSCONFIG_SUCCESS) {
-        set_error(error_to_string(ret));
-        return JS_FALSE;
-    }
-    
-    return JS_TRUE;
+    return JS_ThrowInternalError(ctx, "import_json() not supported by utopia syscfg");
 }
 
 /**
@@ -664,65 +635,46 @@ static JSValue js_sysconfig_export_json(JSContext *ctx, JSValueConst this_val,
     }
     
     char* namespace = make_namespace(db, section);
-    char* json_output = NULL;
-    int ret = sysconfig_export_json(namespace, &json_output);
+    
+    // utopia syscfg doesn't support JSON export
     
     if (db) JS_FreeCString(ctx, db);
     if (section) JS_FreeCString(ctx, section);
     if (namespace) free(namespace);
     
-    if (ret != SYSCONFIG_SUCCESS || !json_output) {
-        set_error(error_to_string(ret));
-        return JS_NULL;
-    }
-    
-    JSValue result = JS_NewString(ctx, json_output);
-    free(json_output);
-    
-    return result;
+    return JS_ThrowInternalError(ctx, "export_json() not supported by utopia syscfg");
 }
 
 /**
  * sysconfig.get_stats()
- * Get system statistics
+ * Get system statistics (not supported by utopia syscfg)
  */
 static JSValue js_sysconfig_get_stats(JSContext *ctx, JSValueConst this_val,
                                       int argc, JSValueConst *argv)
 {
-    int keys_count = 0;
-    size_t memory_used = 0;
-    int dirty_count = 0;
-    
-    int ret = sysconfig_get_stats(&keys_count, &memory_used, &dirty_count);
-    
-    if (ret != SYSCONFIG_SUCCESS) {
-        set_error(error_to_string(ret));
-        return JS_NULL;
-    }
-    
+    // utopia syscfg doesn't provide stats API
+    // Return empty object for compatibility
     JSValue obj = JS_NewObject(ctx);
-    JS_SetPropertyStr(ctx, obj, "keys_count", JS_NewInt32(ctx, keys_count));
-    JS_SetPropertyStr(ctx, obj, "memory_used", JS_NewInt64(ctx, memory_used));
-    JS_SetPropertyStr(ctx, obj, "dirty_count", JS_NewInt32(ctx, dirty_count));
+    JS_SetPropertyStr(ctx, obj, "keys_count", JS_NewInt32(ctx, 0));
+    JS_SetPropertyStr(ctx, obj, "memory_used", JS_NewInt64(ctx, 0));
+    JS_SetPropertyStr(ctx, obj, "dirty_count", JS_NewInt32(ctx, 0));
     
     return obj;
 }
 
 /**
  * sysconfig.get_version()
- * Get version information
+ * Get version information (not supported by utopia syscfg)
  */
 static JSValue js_sysconfig_get_version(JSContext *ctx, JSValueConst this_val,
                                         int argc, JSValueConst *argv)
 {
-    int major = 0, minor = 0, patch = 0;
-    
-    sysconfig_get_version(&major, &minor, &patch);
-    
+    // utopia syscfg doesn't provide version API
+    // Return a dummy version for compatibility
     JSValue obj = JS_NewObject(ctx);
-    JS_SetPropertyStr(ctx, obj, "major", JS_NewInt32(ctx, major));
-    JS_SetPropertyStr(ctx, obj, "minor", JS_NewInt32(ctx, minor));
-    JS_SetPropertyStr(ctx, obj, "patch", JS_NewInt32(ctx, patch));
+    JS_SetPropertyStr(ctx, obj, "major", JS_NewInt32(ctx, 1));
+    JS_SetPropertyStr(ctx, obj, "minor", JS_NewInt32(ctx, 0));
+    JS_SetPropertyStr(ctx, obj, "patch", JS_NewInt32(ctx, 0));
     
     return obj;
 }
@@ -753,8 +705,8 @@ static void context_finalizer(JSRuntime *rt, JSValue val)
 {
     sysconfig_context_t* handle = JS_GetOpaque(val, context_class_id);
     if (handle) {
-        if (handle->database) {
-            free(handle->database);
+        if (handle->namespace_prefix) {
+            free(handle->namespace_prefix);
         }
         free(handle);
     }
@@ -796,17 +748,17 @@ static JSValue js_context_get(JSContext *ctx, JSValueConst this_val,
         return JS_EXCEPTION;
     }
     
-    char* namespace = make_namespace(handle->database, section);
-    char value[SYSCONFIG_MAX_VALUE_LEN];
-    int ret = sysconfig_get(namespace, key, value, sizeof(value));
-    
+    char* namespace = make_namespace(handle->namespace_prefix, section);
+    char value[SYSCFG_MAX_VALUE_LEN];
+    int ret = syscfg_get(namespace, key, value, sizeof(value));
+
     if (section) JS_FreeCString(ctx, section);
     JS_FreeCString(ctx, key);
     if (namespace) free(namespace);
     
-    if (ret == SYSCONFIG_ERROR_NOT_FOUND) {
+    if (ret == ERR_INVALID_PARAM) {
         return JS_NULL;
-    } else if (ret != SYSCONFIG_SUCCESS) {
+    } else if (ret != 0) {
         set_error(error_to_string(ret));
         return JS_ThrowInternalError(ctx, "%s", g_error_msg);
     }
@@ -849,20 +801,25 @@ static JSValue js_context_get_default(JSContext *ctx, JSValueConst this_val,
         return JS_EXCEPTION;
     }
     
-    char* namespace = make_namespace(handle->database, section);
-    char value[SYSCONFIG_MAX_VALUE_LEN];
-    int ret = sysconfig_get_default(namespace, key, default_value, value, sizeof(value));
+    char* namespace = make_namespace(handle->namespace_prefix, section);
+    char value[SYSCFG_MAX_VALUE_LEN];
+    memset(value, 0, sizeof(value));
+    
+    // utopia syscfg doesn't have get_default, so we try get and use default on failure
+    int ret = syscfg_get(namespace, key, value, sizeof(value));
     
     if (section) JS_FreeCString(ctx, section);
     JS_FreeCString(ctx, key);
-    JS_FreeCString(ctx, default_value);
     if (namespace) free(namespace);
     
-    if (ret != SYSCONFIG_SUCCESS) {
-        set_error(error_to_string(ret));
-        return JS_ThrowInternalError(ctx, "%s", g_error_msg);
+    if (ret != 0) {
+        // Failed to get value, return default
+        JSValue result = JS_NewString(ctx, default_value);
+        JS_FreeCString(ctx, default_value);
+        return result;
     }
     
+    JS_FreeCString(ctx, default_value);
     return JS_NewString(ctx, value);
 }
 
@@ -907,15 +864,15 @@ static JSValue js_context_set(JSContext *ctx, JSValueConst this_val,
         return JS_EXCEPTION;
     }
     
-    char* namespace = make_namespace(handle->database, section);
-    int ret = sysconfig_set(namespace, key, value);
+    char* namespace = make_namespace(handle->namespace_prefix, section);
+    int ret = syscfg_set(namespace, key, value);
     
     if (section) JS_FreeCString(ctx, section);
     JS_FreeCString(ctx, key);
     if (value) JS_FreeCString(ctx, value);
     if (namespace) free(namespace);
     
-    if (ret != SYSCONFIG_SUCCESS) {
+    if (ret != 0) {
         set_error(error_to_string(ret));
         return JS_ThrowInternalError(ctx, "%s", g_error_msg);
     }
@@ -953,14 +910,14 @@ static JSValue js_context_unset(JSContext *ctx, JSValueConst this_val,
         return JS_EXCEPTION;
     }
     
-    char* namespace = make_namespace(handle->database, section);
-    int ret = sysconfig_unset(namespace, key);
+    char* namespace = make_namespace(handle->namespace_prefix, section);
+    int ret = syscfg_unset(namespace, key);
     
     if (section) JS_FreeCString(ctx, section);
     JS_FreeCString(ctx, key);
     if (namespace) free(namespace);
     
-    if (ret != SYSCONFIG_SUCCESS) {
+    if (ret != 0) {
         set_error(error_to_string(ret));
         return JS_FALSE;
     }
@@ -998,18 +955,17 @@ static JSValue js_context_exists(JSContext *ctx, JSValueConst this_val,
         return JS_EXCEPTION;
     }
     
-    char* namespace = make_namespace(handle->database, section);
-    int ret = sysconfig_exists(namespace, key);
+    char* namespace = make_namespace(handle->namespace_prefix, section);
+    
+    // utopia syscfg doesn't have exists(), so we try a get instead
+    char value[SYSCFG_MAX_VALUE_LEN];
+    int ret = syscfg_get(namespace, key, value, sizeof(value));
     
     if (section) JS_FreeCString(ctx, section);
     JS_FreeCString(ctx, key);
     if (namespace) free(namespace);
     
-    if (ret < 0) {
-        return JS_FALSE;
-    }
-    
-    return JS_NewBool(ctx, ret);
+    return JS_NewBool(ctx, ret == 0);
 }
 
 /**
@@ -1025,9 +981,9 @@ static JSValue js_context_commit(JSContext *ctx, JSValueConst this_val,
     }
     
     // Note: sysconfig commits globally, not per-database
-    int ret = sysconfig_commit();
+    int ret = syscfg_commit();
     
-    if (ret != SYSCONFIG_SUCCESS) {
+    if (ret != 0) {
         set_error(error_to_string(ret));
         return JS_FALSE;
     }
@@ -1047,14 +1003,8 @@ static JSValue js_context_rollback(JSContext *ctx, JSValueConst this_val,
         return JS_ThrowTypeError(ctx, "Invalid context object");
     }
     
-    int ret = sysconfig_rollback();
-    
-    if (ret != SYSCONFIG_SUCCESS) {
-        set_error(error_to_string(ret));
-        return JS_FALSE;
-    }
-    
-    return JS_TRUE;
+    // utopia syscfg doesn't have rollback - would need to reload from file
+    return JS_ThrowInternalError(ctx, "rollback() not supported by utopia syscfg");
 }
 
 /**
@@ -1069,13 +1019,8 @@ static JSValue js_context_is_dirty(JSContext *ctx, JSValueConst this_val,
         return JS_ThrowTypeError(ctx, "Invalid context object");
     }
     
-    int ret = sysconfig_is_dirty();
-    
-    if (ret < 0) {
-        return JS_FALSE;
-    }
-    
-    return JS_NewBool(ctx, ret);
+    // utopia syscfg doesn't track dirty state
+    return JS_FALSE;
 }
 
 /**
@@ -1116,26 +1061,15 @@ static JSValue js_context_iterate(JSContext *ctx, JSValueConst this_val,
         return JS_ThrowTypeError(ctx, "Third argument must be a function");
     }
     
-    char* namespace = make_namespace(handle->database, section);
-    
-    iterator_data_t data = {
-        .ctx = ctx,
-        .callback = argv[2],
-        .should_stop = 0
-    };
+    char* namespace = make_namespace(handle->namespace_prefix, section);
     
-    int ret = sysconfig_iterate(namespace, pattern, iterator_callback, &data);
+    // utopia syscfg doesn't support iterate
     
     if (section) JS_FreeCString(ctx, section);
     if (pattern) JS_FreeCString(ctx, pattern);
     if (namespace) free(namespace);
     
-    if (ret != SYSCONFIG_SUCCESS) {
-        set_error(error_to_string(ret));
-        return JS_FALSE;
-    }
-    
-    return JS_TRUE;
+    return JS_ThrowInternalError(ctx, "iterate() not supported by utopia syscfg");
 }
 
 /**
@@ -1161,17 +1095,14 @@ static JSValue js_context_count(JSContext *ctx, JSValueConst this_val,
         if (!section) return JS_EXCEPTION;
     }
     
-    char* namespace = make_namespace(handle->database, section);
-    int ret = sysconfig_count(namespace);
+    char* namespace = make_namespace(handle->namespace_prefix, section);
+    
+    // utopia syscfg doesn't support count
+    int ret = 0;
     
     if (section) JS_FreeCString(ctx, section);
     if (namespace) free(namespace);
     
-    if (ret < 0) {
-        set_error(error_to_string(ret));
-        return JS_NewInt32(ctx, 0);
-    }
-    
     return JS_NewInt32(ctx, ret);
 }
 
@@ -1198,22 +1129,14 @@ static JSValue js_context_export_json(JSContext *ctx, JSValueConst this_val,
         if (!section) return JS_EXCEPTION;
     }
     
-    char* namespace = make_namespace(handle->database, section);
-    char* json_output = NULL;
-    int ret = sysconfig_export_json(namespace, &json_output);
+    char* namespace = make_namespace(handle->namespace_prefix, section);
+    
+    // utopia syscfg doesn't support JSON export
     
     if (section) JS_FreeCString(ctx, section);
     if (namespace) free(namespace);
     
-    if (ret != SYSCONFIG_SUCCESS || !json_output) {
-        set_error(error_to_string(ret));
-        return JS_NULL;
-    }
-    
-    JSValue result = JS_NewString(ctx, json_output);
-    free(json_output);
-    
-    return result;
+    return JS_ThrowInternalError(ctx, "export_json() not supported by utopia syscfg");
 }
 
 /**
@@ -1243,16 +1166,10 @@ static JSValue js_context_import_json(JSContext *ctx, JSValueConst this_val,
         return JS_EXCEPTION;
     }
     
-    int ret = sysconfig_import_json(json_data, merge);
-    
+    // utopia syscfg doesn't support JSON import
     JS_FreeCString(ctx, json_data);
     
-    if (ret != SYSCONFIG_SUCCESS) {
-        set_error(error_to_string(ret));
-        return JS_FALSE;
-    }
-    
-    return JS_TRUE;
+    return JS_ThrowInternalError(ctx, "import_json() not supported by utopia syscfg");
 }
 
 /**
@@ -1267,7 +1184,7 @@ static JSValue js_context_database(JSContext *ctx, JSValueConst this_val,
         return JS_ThrowTypeError(ctx, "Invalid context object");
     }
     
-    return JS_NewString(ctx, handle->database);
+    return JS_NewString(ctx, handle->namespace_prefix);
 }
 
 /**
@@ -1309,10 +1226,10 @@ static JSValue js_sysconfig_context(JSContext *ctx, JSValueConst this_val,
         return JS_ThrowOutOfMemory(ctx);
     }
     
-    handle->database = strdup(db);
+    handle->namespace_prefix = strdup(db);
     JS_FreeCString(ctx, db);
     
-    if (!handle->database) {
+    if (!handle->namespace_prefix) {
         free(handle);
         return JS_ThrowOutOfMemory(ctx);
     }
@@ -1320,7 +1237,7 @@ static JSValue js_sysconfig_context(JSContext *ctx, JSValueConst this_val,
     // Create JS object with context class
     JSValue obj = JS_NewObjectClass(ctx, context_class_id);
     if (JS_IsException(obj)) {
-        free(handle->database);
+        free(handle->namespace_prefix);
         free(handle);
         return obj;
     }
