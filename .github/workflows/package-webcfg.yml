name: Package webcfg

on:
  push:
    branches: [ main ]
    paths:
      - 'packages/webcfg/**'
      - 'scripts/**'
      - '.github/workflows/package-webcfg.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'packages/webcfg/**'
      - 'scripts/**'
      - '.github/workflows/package-webcfg.yml'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        platform: [arm64, amd64]
        include:
          - platform: arm64
            docker_platform: linux/arm64
          - platform: amd64
            docker_platform: linux/amd64
    
    runs-on: ubuntu-latest
    name: Build webcfg for ${{ matrix.platform }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU for multi-arch builds
        uses: docker/setup-qemu-action@v3
        with:
          platforms: linux/amd64,linux/arm64

      - name: Build package in Docker
        run: |
          docker run --rm --platform=${{ matrix.docker_platform }} \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            ubuntu:20.04 \
            bash -c '
              set -euo pipefail
                            
              # Source build helpers
              source scripts/build-helpers/build-common.sh
              
              # Install dependencies for this package
              install_build_dependencies webcfg
              
              # Build the package
              build_package webcfg ${{ matrix.platform }}
            '

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: webcfg-${{ matrix.platform }}
          path: |
            webcfg_*_${{ matrix.platform }}.deb
            libwebcfg_*_${{ matrix.platform }}.deb
            libwebcfg-dev_*_${{ matrix.platform }}.deb
