name: Package blizzard-tinyshell

on:
  workflow_dispatch:
    inputs:
      upstream_ref:
        description: 'Upstream tag/branch/commit (default: HEAD)'
        required: false
  push:
    paths:
      - 'packages/blizzard-tinyshell/**'
      - '.github/workflows/package-blizzard-tinyshell.yml'
      - 'scripts/build-helpers/**'

permissions:
  contents: write
  packages: write

jobs:
  build:
    name: Build blizzard-tinyshell for ${{ matrix.platform }}
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: amd64
            docker_platform: linux/amd64
          - platform: arm64
            docker_platform: linux/arm64
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: linux/amd64,linux/arm64
      
      - name: Download dependencies
        uses: actions/download-artifact@v4
        with:
          pattern: |
            blizzard-cpp-sdk-${{ matrix.platform }}-packages
            blizzard-rdk-${{ matrix.platform }}-packages
          path: .
      
      - name: Build package in Docker
        run: |
          docker run --rm --platform=${{ matrix.docker_platform }} \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            ubuntu:20.04 \
            bash -c '
              set -euo pipefail
              
              # Install base dependencies
              export DEBIAN_FRONTEND=noninteractive
              apt-get update -qq
              apt-get install -y -qq \
                git \
                build-essential \
                cmake \
                pkg-config \
                file \
                binutils \
                dpkg-dev
              
              # Install blizzard dependencies
              dpkg -i blizzard-cpp-sdk_*_${{ matrix.platform }}.deb || apt-get install -f -y
              dpkg -i libblizzard-cpp-sdk-dev_*_${{ matrix.platform }}.deb || apt-get install -f -y
              dpkg -i blizzard-rdk_*_${{ matrix.platform }}.deb || apt-get install -f -y
              
              # Source build helpers
              source scripts/build-helpers/build-common.sh
              
              # Build package
              build_package "blizzard-tinyshell" "${{ matrix.platform }}" "${{ inputs.upstream_ref }}"
            '
      
      - name: List generated packages
        run: |
          echo "Generated packages:"
          ls -lh *.deb || echo "No packages found"
      
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: blizzard-tinyshell-${{ matrix.platform }}-packages
          path: '*.deb'
          if-no-files-found: error
