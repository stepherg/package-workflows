name: Package libsafec

on:
  push:
    branches: [ main ]
    paths:
      - 'packages/libsafec/**'
      - 'scripts/**'
      - '.github/workflows/package-libsafec.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'packages/libsafec/**'
      - 'scripts/**'
      - '.github/workflows/package-libsafec.yml'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        platform: [arm64, amd64]
        include:
          - platform: arm64
            docker_platform: linux/arm64
          - platform: amd64
            docker_platform: linux/amd64
    
    runs-on: ubuntu-latest
    name: Build libsafec for ${{ matrix.platform }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU for multi-arch builds
        uses: docker/setup-qemu-action@v3
        with:
          platforms: ${{ matrix.docker_platform }}

      - name: Build package in Docker
        run: |
          docker run --rm --platform=${{ matrix.docker_platform }} \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            debian:bookworm \
            bash -c '
              set -euo pipefail

              # Source build helpers
              source scripts/build-helpers/build-common.sh
              install_build_dependencies libsafec
              
              # Build package
              build_package libsafec ${{ matrix.platform }}
            '
